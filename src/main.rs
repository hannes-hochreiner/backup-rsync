fn main() {
    println!("Hello, world!");
    // let to_be_deleted = policer::police(&Utc::now().into(), policy, &list[..]);
    // remove snapshots
    // for (_, delete) in to_be_deleted {
    // }
}
